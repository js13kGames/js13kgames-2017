(function() {
    'use strict';

    var bridge = {};

    var roomState = {
    };

    bridge.init = function() {
    };

    bridge.update = function() {
    };

    bridge.render = function() {
    };

    bridge.name = 'bridge';
    muri.rooms.push(bridge);
}());
