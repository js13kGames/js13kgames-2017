(function() {
    'use strict';

    var engine = {};
    
    var roomState = {
    };

    engine.init = function() {
    };

    engine.update = function() {
    };

    engine.render = function() {
    };

    engine.name = 'engine';
    muri.rooms.push(engine);
}());
